{
  "name": "dotfiles",
  "private": true,
  "scripts": {
    "start": "npm run sync",
    "sync": "node ./scripts/sync.js -df",
    "restore": "node ./scripts/sync.js -dr"
  },
  "devDependencies": {
    "@wuxh/backup-cli": "1.x"
  }
}